{
  "name": "versioning-client",
  "version": "0.0.0",
  "description": "Client part of versioning demo project.",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc --build",
    "clean": "rimraf dist tsconfig.tsbuildinfo",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Guy Yafe",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "rimraf": "^5.0.1"
  }
}
